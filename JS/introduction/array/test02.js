const students = ["lika","liya","rotha","panha","Bunna"];

//remove the element for array with pop function
// students.pop();

//remove the element for array with shift function
// students.shift();

//remove the element for array with splice function
students.splice(2,1); // remove rotha at index 2

console.log(students);
